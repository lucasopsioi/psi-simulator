# FDE 架构评审：FSD PSI 推演 / SO 推演

> 2026-09-15 · 基于 V83 代码与实测数据。评审口径：把它当作要交付给客户、由别人接手维护、要跑三年的生产软件来看，而不是个人工具。
> 结论先行：**引擎层（CORE）是可信资产，其余是技术负债。** 数值正确性目前靠「指纹基线 + 43 项自检」兜住，这是它最大的优点，也是它唯一的安全网。

---

# 一、硬数据

| 指标 | 实测 | 意味着什么 |
|---|---|---|
| 单文件行数 / 体积 | 15,884 行 / 1.09 MB | 已超过任何人能整体理解的尺度；改动靠字符串锚点打补丁 |
| 引擎（CORE）行数 | 2,671 行，109 个纯函数 | 可单测、可复用的部分只占 17% |
| 页面层函数 | 约 880 个 | 无模块边界，全部同一作用域 |
| `state.*` 字段 | 55 个 | 全局可变状态，任何函数都能写 |
| `window.__*` 全局 | 41 个 | 渲染、计算、自检互相通过全局对象传递 |
| `render()` 调用点 | 205 处 | 任何交互 = 整页重绘 |
| `innerHTML=` | 78 处 | 手拼 HTML 字符串，无组件、无转义约束 |
| `$('#id')` 全局查询 | 267 处 | 同 id 控件在页面里不能出现两份，合并页面时踩过 |
| `catch(e){}` 吞错 | 49 处 | 错误静默，用户看到「数不对」而不是「哪里错了」 |
| 页面层 `Math.round` | 218 处 | 取整规则散落，非 5 倍数的 bug 就是这样产生的 |
| 中文键硬编码 | 96 处 | `'音频与智能配件'`、`'手机'` 等字面量参与逻辑判断 |
| 缓存键 | 47 个 | 六套缓存各自失效，靠三个代际计数器手工维护 |
| 单测断言 / 自检标志 | 552 / 43 | 引擎覆盖好；页面层靠指纹基线（37 视图 + 20 导出） |
| 版本控制 | **无 git** | 每版靠 exe 文件名 + README 记录，无法 diff、无法回退 |
| 主进程 IPC | 14 个 handler，555 行 | 合理 |

---

# 二、架构问题（按严重度）

## A1 · 没有版本控制（P0）

15,884 行、82 个版本、9 天里改了 13 版，全部没有 git。回退靠 README 的文字描述和补丁脚本的锚点。任何一次误改（比如上周 `\\n` 被 heredoc 吃掉那种）都不可追溯。
**这是所有其他问题的放大器**：没有 diff，评审、回退、并行改动、交接都做不到。

## A2 · 单文件、无模块、全局作用域（P0）

一个 HTML 里装了引擎、状态、17 个页面渲染器、导入导出、AI 助手、自检、探针。改任何一处都要靠 Python 脚本用字符串锚点替换——这次 13 版里锚点失败了 4 次，靠 assert 拦住。
后果：① 新人无法接手；② 无法按需加载（SO 工具带着整套 PSI 代码跑）；③ 编辑器/工具链对 1MB 单文件基本失效。

## A3 · 全局可变状态 + 整页重绘（P1）

`state` 55 个字段谁都能写，205 处 `render()` 整页重建 DOM 并重绑事件。V64–V70 的性能战役（拍数 302ms → 4ms）本质上是在绕开这个模型：增量重算 + 原地打补丁 `pv2Patch`。这套优化正确但脆弱——它依赖 `data-*` 属性和 `cellsByNode` 索引与整页渲染保持一致，两边任一改动都可能静默失效。

## A4 · 计算与渲染耦合（P1）

渲染期写计算结果：`budgetRowsOv` 在渲染时给 `__lockRes` 追加字段；`it.__pvv` 缓存挂在计算对象上；`digest()` 依赖 `__lockRes`。这意味着「同一份数据，先看哪个页，结果可能不同」的风险真实存在（方案里已列为 `lockResOk` 门禁，尚未落）。

## A5 · 缓存与失效靠人（P1）

六层缓存（`calcCache` 47 键、`__budUnitCache`、`__budRowsCache`、`__pv2VC`、`it.__pvv`、`lyModels`），失效由 `dataGen / simGen / ovExtGen` 三个计数器加各处手工 `delete` 维护。V70 的 `extDirty` 推迟、V72 的 `budCacheEvict` 都是补丁。任何新缓存都要人记得在正确的地方失效。

## A6 · 两个产品靠运行时开关分叉（P2）

SO-Sim 与 PSI 是同一个 HTML，靠 `soTool()` 在 20 多处 `if` 分叉。现在能工作是因为差异小；每多一处差异，就多一对「PSI 改了 SO 没改」的坑。自检为此已经要用 `__soToolOff/__soToolForce` 两个开关切换身份。

## A7 · 测试形态倒挂（P2）

引擎有 552 条单测（好）。页面层没有单测，靠端到端自检 + 指纹基线。指纹基线能证明「没变」，不能证明「对」；43 项自检里有相当部分是在验证自检本身的假设（比如 SO 模式中立）。没有 CI，门禁靠人记得跑（这次就漏过一次）。

## A8 · 错误处理与可观测性（P2）

49 处 `catch(e){}`。用户遇到的问题（「不是 5 的倍数」「待确认太多」「切换季度 DOS 变了」）都是靠用户发现的，软件自己不报。没有日志文件、没有异常上报、没有「这个数怎么来的」的一等公民（右键 `pv2ShowLogic` 是事后补的）。

## A9 · 隐私边界（P2）

AI 助手可把 SO/SI/库存发往在线模型。V71 已改默认本机 + 首次确认，但「确认一次永久生效」对一个承诺「数据不出本机」的软件仍偏松。

---

# 三、数据整理问题

## D1 · 键是中文拼接字符串（P0）

单元键 = `国家::账户::型号`，外推键 = `国家账户型号`，型号键 = `产品||型号`，层级键 = `src|#|线|#|产品|#|国家|#|渠道`，财经键 = `传播名国家`。**五种键体系**，全部由业务名称拼成。渠道改名、型号改名、底表列名微调，都会让手拍、锁量、监控、连线、财经参数失联。改名迁移有三处实现（`renameChannel / migrateAccountKeys / migrateNameExtras`），还漏了 `chanTree / chanType`。

## D2 · 底表没有契约（P0）

导入靠中文表头启发式匹配（`'年底DOS'`、`r[0]==='音频与智能配件'`），没有 schema、没有版本、没有拒绝。一列改名就是静默错数。三张底表（FSD / 下游 / 音频）形状不同，派生对象 `D / Dr / Da` 的行字段也不同（`si` vs `buy`），页面层用 `src==='fsd'?r.si:r.buy` 到处判。

## D3 · 时间轴是自造编码（P1）

周期 = 整数 `YYYYWWW`（如 `2026035`），ISO 周、周四归月、53 周年、去年同周映射（`refPeriodOf`）全是自写规则，散在引擎 20 多个函数里。正确性靠单测覆盖了主要边界，但没有一处集中的「日历」对象，任何跨年/周数变化的需求都要全文搜。

## D4 · 口径有注册表，没有强制（P1）

`C.CALIBER` 12 个口径 id 已建，但只用于列头 title。同一个「DOS」在 6 个函数里各算一遍（`monthDos / bucketDos / unitCurrent.dosCalc / watchMetrics.dosNow / targetCalc / finCompute`），历史月末库存有 5 种取法（方案 D2 未拍板）。指纹基线冻结了「现状」，包括现状里的不一致。

## D5 · 手拍层语义耦合（P1）

四个袋（`ovSO / ovSI / ovTail / ovExt`）+ 承接比例 + 清尾重置 + 锁量优先 + tapcover 补货，语义互相影响。「拍一个数」会改后续各期（承接）、会补进货（tapcover）、会被库存压回（capped）——这正是用户「看不懂内部怎么算」的直接来源。V80 的 SO 工具把展示收窄了，但语义没有收窄。

## D6 · 缺少数据血缘（P1）

一格数不能反推出它由哪些输入、经过哪些规则得到。现有的右键解释是渲染期临时拼的，不是计算期产出的。用户提「置信度低的太多」「为什么不是 5 的倍数」时，都要我重新读代码去解释。

## D7 · 夹具与基线单薄（P2）

自检夹具只有 10 个 FSD 单元、7 个下游、4 个音频，没有目标表、监控项、新品、锁量；这些路径靠自检临时构造。指纹基线（views 243KB / exports 291KB）是二进制式的「全对或全错」，它挡不住「夹具里没出现的情况」。

## D8 · 持久化没有版本（P2）

`state` 整包 JSON 落盘，迁移逻辑散在 `boot` 里（`if(state.cfg.roundTo===1)…`、`if(!state.ai)…`），没有 schema 版本号，没有向下兼容测试。快照与方案 JSON 是两套覆盖范围不同的持久化。

---

# 四、建议（严谨版：做什么 / 为什么 / 代价 / 怎么验）

## P0 — 本周就该做，不做其余都是空中楼阁

**R1 立即 git init，把当前 V83 作为第一个提交。**
做法：仓库根 = `FSD推演/`；`dist*/`、`node_modules/` 进 `.gitignore`；`baseline/` 进仓库。每个版本一个 tag（`v83`）。
收益：diff、回退、评审、交接全部从零变成有。代价：10 分钟。风险：无。
验证：`git log` 能对上 README 的版本记录。

**R2 底表契约化。**
做法：为三张底表各写一个 schema（列名 → 内部字段、类型、必填、单位），导入时校验，不符合就拒绝并列出缺哪一列；把 96 处中文字面量集中到一个 `DICT` 常量表（产业名、音频线名、渠道类型标签）。
收益：一列改名从「静默错数」变成「导入时报错」。代价：2 天。风险：低（只加校验不改计算）。
验证：把夹具表头改一个字，导入必须拒绝；改回必须通过且指纹不变。

**R3 单元键改为不含业务名称的稳定 id。**
做法：导入时为每个 `国家::账户::型号` 生成 `uid`（首次出现时分配，持久化 `uidMap`），所有手拍/锁量/监控/连线/财经参数改按 uid 存；旧档一次性迁移。
收益：改名不再丢数据；五种键体系收敛为一种；三处迁移代码删掉。代价：4–5 天，是这份清单里最重的一项。风险：中——迁移必须可逆。
验证：旧快照 / 旧 JSON / localStorage 三条路径启动后 `digest()` 与迁移前逐字节相同；改名一次后手拍/锁量/监控/连线全部仍命中。（方案 D14 的内容，我之前建议「放最后可不做」，**现在改为 P0**：FDE 视角下这是数据完整性问题，不是整洁度问题。）

## P1 — 一个月内

**R4 拆文件，不拆架构。**
做法：不做框架迁移。把单文件按现有边界物理拆成 `core/`（引擎）、`state/`（状态 + 持久化 + 迁移）、`views/`（每页一个文件）、`selftest/`、`ai/`，用一个 30 行的 build 脚本拼回单文件发布（保持单文件 exe 的部署优势）。
收益：编辑器能用、diff 可读、SO 工具可以只拼需要的视图（不再靠运行时开关）。代价：3 天。风险：低（纯物理移动，拼回后指纹必须逐字节相同）。
验证：拼回的 HTML 与拆分前 `sha256` 相同（第一次），之后用指纹基线。

**R5 把「解释」做成计算期产物。**
做法：`buildForecastForUnit` 每周输出 `{so, src, base, k, g, carry, cap}` 五个因子（引擎已经有这些中间量，只是没保留）；`simulateUnit` 追加 `{capped, tapcover, tail}` 原因码。矩阵任意格点开显示「基线 × 系数 × 增速 × 承接 → 约束前 → 约束后」。
收益：用户不用再问「这个数怎么来的」；每个 bug 报告自带血缘。代价：2 天。风险：低（只增字段）。
验证：解释面板里四因子相乘取整 = 显示值，对全部单元全部周做断言（进自检）。

**R6 口径强制。**
做法：把 6 处 DOS 计算收敛到 `C.CALIBER` 每个 id 一个实现，页面层禁止再写 `inv/daily`；把 218 处 `Math.round` 里涉及台数的收敛到 `C.round5 / quantizeSeq`。历史月末库存五选一（方案 D2）拍板后只留一种。
收益：同名同数；「不是 5 的倍数」这类 bug 从根上消失。代价：3 天。风险：中——每收敛一处都可能改数，必须逐项过指纹并记录。
验证：指纹差异集合必须能一一对应到拍板条目，多一格都不行。

**R7 CI 化门禁。**
做法：`ship.sh` 已把门禁串进发布；再加一步 `pre-commit` 跑 `node test.js`，`git tag` 前必须 `gate.js --exe` 通过。夹具补齐目标表、监控项、新品、锁量各一个（10 分钟的事）。
收益：这次「门禁 FAIL 照样发布」不会再发生。代价：半天。

## P2 — 一个季度内

**R8 SO 工具物理独立。** 依赖 R4。SO-Sim 只拼 `core + state + views/pv + views/confirm + views/rules`，去掉 20 多处 `soTool()` 分叉。

**R9 缓存收敛。** 用一个 `memo(key, deps)` 统一六层缓存，deps = `[dataGen, simGen, ovExtGen, cfgHash]`，不再手工 `delete`。

**R10 错误可见。** 49 处 `catch(e){}` 改为 `catch(e){ report(e) }`，落本地日志 `userData/errors.log`；设置页加「最近错误」面板。

**R11 持久化加版本号。** `state.schema=1`，迁移集中到 `migrate(from, to)` 一个函数，配旧档回归测试。

**R12 隐私默认收紧。** 在线 provider 每次会话确认一次，而不是永久；工具返回给模型的数据打「已脱敏」标记（型号名保留、渠道名哈希）。

## 明确不建议做的

- **不要迁移到 React/Vue 等框架。** 收益是理论上的，代价是把 2,671 行可信引擎之外的一切重写一遍，指纹基线会全部失效。先做 R4 的物理拆分。
- **不要现在重写引擎。** CORE 是这套软件里唯一有 552 条单测、且经过 8 轮审计的部分。
- **不要用 AI 来「解释」数据代替 R5。** 解释必须是计算期的确定性产物，不能是事后生成的文本。

---

# 五、90 天路径

| 周 | 做什么 | 验证 |
|---|---|---|
| 1 | R1 git、R7 CI 化门禁 + 夹具补齐 | tag v83；夹具覆盖目标/监控/新品/锁量 |
| 2–3 | R2 底表契约 + 字面量字典 | 改表头必拒；指纹不变 |
| 4–5 | R4 物理拆文件 + 拼回 | sha256 相同；之后指纹 |
| 6 | R5 计算期血缘 + 解释面板 | 四因子断言进自检 |
| 7–8 | R3 uid 键迁移 | 三条启动路径 digest 相同 |
| 9–10 | R6 口径强制（含 D2 拍板） | 指纹差异 = 拍板条目 |
| 11–12 | R8 SO 工具物理独立、R9 缓存收敛 | 两个产物各自过门禁 |
| 13 | R10–R12 | 错误日志、schema 版本、隐私 |

每一步都有回退点（git tag）和通过标准（指纹或 sha256）。顺序不能换：没有 R1 就没有回退；没有 R2/R3 的键稳定，R4 之后的每次重构都在流沙上。
